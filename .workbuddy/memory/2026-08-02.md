# 2026-08-02 工作日志

## xhermes fork 设计文档审查

审查了 `docs/superpowers/specs/2026-08-02-xhermes-fork-design.md`（hermes-agent fork 为 xhermes-agent 的共存设计）。

通过对照实际代码库核查，确认文档总体严谨，量化声明经得起验证（HERMES_ env 读取 965 处、~/.hermes 字面量 1753 处，均支持其"不改模块名/不批量改 env"的决策）。

发现 2 个实质性遗漏：
1. `scripts/hermes-gateway` 独立脚本完全未列入改动清单——该脚本硬编码 `SERVICE_NAME="hermes-gateway"`、`ai.hermes.gateway.plist` 路径/标签、且用 `PROJECT_DIR/venv` 作 venv 路径，会破坏共存。
2. `hermes_constants.py` 的 `get_default_hermes_root()`（L161-198）直接读 `HERMES_HOME`（不经 `_hermes_home_from_env`），绕过文档提议的 `XHERMES_HOME` 优先层；若 HERMES_HOME 泄漏，会把 `~/.hermes` 当作 xhermes root 返回。

次要遗漏：`ai.hermes.gateway` 标签另见于 `gateway/restart_loop_guard.py`、`gateway/run.py:10213`、`tools/approval.py:760`、`hermes_cli/config_defaults.py:2460`，§3.3 "全集"表未覆盖。

## 修复设计文档

已对 `docs/superpowers/specs/2026-08-02-xhermes-fork-design.md` 执行 5 处编辑：
1. 新增 §3.2b-1：`get_default_hermes_root()` 绕过 `XHERMES_HOME` 的修复要求 + 验证条件
2. §3.3 表格补 `scripts/hermes-gateway` 独立脚本行（SERVICE_NAME/plist/label/venv）
3. §3.3 补 `ai.hermes.gateway` 5 个额外出现点表（含 approval.py L760 比对风险）
4. §5.4 checklist regex 增 `SERVICE_NAME = "hermes` 模式
5. §7 风险清单增 2 条；§9 修订记录增二次修订条目
