# 2026-07-30

## Task Relay design review (docs/superpowers/specs/2026-07-31-task-relay-design.md)

Reviewed rev 5 of the Task Relay distributed multi-agent dispatch design.

Verification against codebase (via Explore agent):
- `delegate_task` (tools/delegate_tool.py:2778) has `goal`/`context` but NO `toolsets` param (inherits parent) and NO `timeout_seconds` param (config-driven via delegation.child_timeout_seconds). Design's "aligns with goal/context/toolsets/timeout_seconds" claim is inaccurate on 2/4 fields.
- Kanban (hermes_cli/kanban_db.py) is local HERMES_HOME subprocess profiles — "sibling, local-only" claim accurate.
- ACP (acp_adapter/session.py:199 create_session, server.py:1528 prompt, server.py:1440 cancel sets cancel_event + agent.interrupt()) — design's ACP claims all accurate. AIAgent.interrupt() at run_agent.py:2937.
- Gateway relay wake (docs/relay-connector-contract.md §3.3): wake poke is UNSIGNED GET; HMAC is on WS upgrade and NOT single-use. Design's "HMAC single-use tokens" pattern-reuse claim conflates two mechanisms.
- Footprint Ladder (AGENTS.md:182-211): extend → CLI+skill → service-gated tool → plugin → MCP → core tool. Design uses CLI subcommands (rung 2) — defensible but heavy.
- hermes tools/setup auto-install confirmed (hermes_cli/main.py:10897, 2978).

Key review findings: design is architecturally sound (clean responsibility split, good idempotency, honest cooperative-cancel model, L1/L2 checkpoint distinction), but has ~15 issues ranging from factual inaccuracies to footprint/security concerns. Most material: delegate_task alignment claim wrong; gateway wake reuse misstated; ~4600 LoC in-tree footprint needs explicit justification vs plugin path; hermes_cli/main.py god-file modification; base static-bearer security risky to ship as default for cross-network tool execution; first_progress_seconds=60s too tight for ACP cold start; global event_id sequence is HA contention point.

## Applied must-fix edits (rev 5 → rev 6)

User asked to fix the four must-fix items. Applied to docs/superpowers/specs/2026-07-31-task-relay-design.md, bumped to rev 6 with changelog entry:

- **P0-1** (Relationship table, line 46): corrected `delegate_task` alignment — only `goal`/`context` borrowed; `toolsets`/`timeout_seconds` deliberately promoted to explicit fields (remote workers cross trust boundaries, must not inherit parent toolset). Cited tools/delegate_tool.py.
- **P0-2** (Relationship table line 49 + Mode B wake note ~line 172): corrected gateway-relay-wake reuse claim — wake poke is unsigned payload-free GET; HMAC is per-gateway WS-upgrade secret (not single-use); Task Relay's single-use wake token is independently designed; only outbound-only connectivity shared.
- **P1-3** (proto line 284, JSON example line 714, Lease section ~764, timeout table ~1231): raised `first_progress_seconds` default 60→120 across all 4 sites; added mandatory "Immediate claim-ack progress frame" rule — worker MUST emit task.progress before first model round-trip to clear deadline independent of ACP cold-start latency.
- **P1-4** (module table ~115, Security table ~1499, JWT claims label, worker.announce ~596): promoted worker JWT + Master→Hub JWT from M3 (optional) to M1 baseline; static worker_secret bearer declared not shippable for cross-network tool execution; worker.announce body drops worker_secret (JWT as bearer on WS upgrade); mTLS stays optional M3.

Verified no stale `worker_secret`-as-base, `default 60`, or `pattern reuse`/`aligns with delegate` references remain (grep clean except intentional rationale note).

## Second review pass — 4 additional bugs + 1 from own rev 6

User flagged 4 bugs found in current doc (post rev 6). Verified all 4 are real:

- **S1** (`session_mode` singular vs capability set): REAL. Connection Modes text (line 123-125) says "Modes are capabilities, not mutually exclusive: a worker may support A+C, or A+B". But proto (line 533: `string session_mode = 3; // short | long`), flow diagrams (lines 154, 182, 592, 667), JSON example (line 1114), and CLI sketch (line 1389: `--session-mode long`) all use singular. Should be `repeated SessionMode` + enum, or repeated string flags.
- **S2** (`task_events.task_id` NOT NULL vs AGGREGATE): REAL. SQLite schema line 1332 has `task_id TEXT NOT NULL`, but TaskEvent comment line 351 explicitly says "AGGREGATE events describe a GROUP, not a task. task_id is empty on them." Schema should make task_id nullable, or enforce NOT NULL only when kind != AGGREGATE.
- **S3** (`resume_formats` position mismatch): REAL. WorkerInfo proto line 538 has `repeated string resume_formats = 8` at top level; but JSON example line 1123 nests it inside `capabilities`. Pick one and align both.
- **S4** (CLI DB path not using HERMES_HOME): REAL. Line 1383 `--db /path/to/tasks.db` uses hardcoded path; established pattern is `get_hermes_home()` (verified at hermes_cli/sessions_cmd.py:34). Should be `--db $HERMES_HOME/relay/tasks.db` or default to `get_hermes_home() + /relay/tasks.db`.

Also flagged one inconsistency I introduced in my own P1-4 fix:
- **S5** (line 1388 `--worker-secret "$TASK_WORKER_SECRET"`): contradicts the rev 6 rationale that static `worker_secret` is "not a shippable base" for cross-network tool execution. CLI sketch still shows the old flag. Should be JWT-based (e.g. `--worker-jwt-file` or token-endpoint-based issuance).

Awaiting user decision on whether to fix these 4 (+ S5) one by one or all at once.

## Applied rev 7 — S1-S5 consistency fixes

User confirmed ("OK"). Bundled all 5 as rev 7:

- **S1** `session_mode` singular → capability set. Added `enum SessionMode { A=1; B=2; C=3; }` after WorkerInfo; changed `string session_mode = 3` → `repeated SessionMode session_modes = 3` (A always present, mandatory). Updated 11 sites: proto, 4 flow diagrams (lines 154/182/596/667), Connection Modes text (123), JSON example (1114→session_modes:["a","c"]), workers table schema (session_modes TEXT DEFAULT 'a'), metrics label (relay_worker_sessions_active), CLI flag (--session-modes a,c).
- **S2** `task_events.task_id` NOT NULL → nullable + `CHECK (kind='AGGREGATE' OR task_id IS NOT NULL)`. AGGREGATE rows describe a group, task_id empty per TaskEvent contract. (checkpoints.task_id stays NOT NULL — every checkpoint belongs to a task, correct.)
- **S3** `resume_formats` moved out of announce `capabilities` block to JSON top level, matching WorkerInfo proto field 8. Also fixed line 910 text "advertised in capabilities" → "advertised in `resume_formats`".
- **S4** CLI `--db /path/to/tasks.db` → `--db "$HERMES_HOME/relay/tasks.db"` (default via get_hermes_home()).
- **S5** CLI `--worker-secret` → `--worker-jwt-file "$HERMES_HOME/relay/worker.jwt"` + note worker obtains JWT from Hub token endpoint, caches + refreshes before exp. Aligns with rev 6 P1-4 rationale.

Rev header bumped 6→7, changelog entry added. Final grep clean: no stale singular session_mode, no --worker-secret flag (only rationale note), no --db /path, task_id NOT NULL only in checkpoints (correct).

## Applied rev 8 — P1-5/6/7 + P3-12/13/14/15 (limits & policy)

User asked to fix remaining P1 + P3 items. Bundled as rev 8:

- **P1-5** resume_blob size cap: `resume_blob_max_bytes` (default 1 MiB, config.yaml); Hub rejects oversized checkpoint with INVALID_ARGUMENT, worker falls back to ContextRef. Added proto comment (field 7), L2 contract bullet, Timeout Layers table row.
- **P1-6** global event_id HA bottleneck: clarified M1 scope = single Hub instance owns AUTOINCREMENT/SEQUENCE (uncontended); multi-Hub HA turns sequence into write-contention point; partitioned/hybrid sequence deferred to M3, MUST preserve globally-monotonic cross-filter comparability. Added "HA scope" paragraph after Cursor semantics.
- **P1-7** WatchTask backpressure: bounded per-stream buffer `watch_stream_buffer_events` (default 1024); on overflow Hub closes stream with gRPC RESOURCE_EXHAUSTED + new `SlowConsumer{delivered_event_id, newest_event_id}` proto; never blocks production, never drops silently; Master reconciles via GetTaskResult/ListTasks then resubscribes.
- **P3-12** AggregateResult.metrics collision: concatenation not merge; added `origin_task_id` field 5 to Metric proto (populated by Hub when aggregating); Hub MUST NOT collapse same-named metrics; Master disambiguates by task, sums client-side. Added "Metric name collisions" paragraph.
- **P3-13** ListTasks.limit cap: default 100, max `list_tasks_max_limit` (default 500); Hub clamps over-limit. Updated proto field 6 comment.
- **P3-14** Mode C cancel credit return: cancelled task refunds one credit exactly like task.complete; only never-acknowledged push (lost on disconnect) recovered at `lost` settlement; preserves invariant `granted = in_flight + available`. Added "Credit return on cancel" paragraph.
- **P3-15** min_resources hard gate: not a preference — unspecified worker field = unsatisfied (excluded, never best-effort); unsatisfiable → permanent pending until queue_timeout_seconds → lost; soft preferences belong in params/hints. Added proto comment to ResourceRequirements + rewrote "Self-reported resources are advisory" paragraph + annotated scoring order steps 2/3 as hard gates.

Rev header bumped 7→8, changelog entry added. Final grep confirms all 7 new knobs/structures present: resume_blob_max_bytes, watch_stream_buffer_events, SlowConsumer message, origin_task_id, list_tasks_max_limit, HA scope para, Credit return on cancel para, hard gate para.

Task Relay design now at rev 8. All P0/P1/P3 review items resolved; only P2 items (in-tree footprint justification, hermes_cli/main.py god-file, Go Hub twin trigger criteria, worker_secret env-vs-config boundary) remain as deferred design decisions, not bugs.

