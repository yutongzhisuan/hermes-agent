"""Agent Gateway — JSON-RPC over WebSocket (headless ``xhermes serve``).

Public name: **Agent Gateway**. The Python package remains ``tui_gateway`` for
import stability; third-party hosts should connect via ``/api/ws`` documented in
``website/docs/developer-guide/desktop-gateway-protocol.md``.
"""
